import { Box, Typography } from '@mui/material';

export default function TestPage() {
  return (
    <Box>
      <Typography variant="h4">Test Sayfası</Typography>
      <Typography>Eğer bunu görüyorsanız, React çalışıyor! 🎉</Typography>
    </Box>
  );
}
